(ns dda.pallet.webmeeting.convention-test
  (:require
   [clojure.test :refer :all]
   [data-test :refer :all]
   [dda.pallet.webmeeting.convention :as sut]))

;; (defdatatest should-generate-infra-for-domain [input expected]
;;   (is (= expected
;;          (sut/infra-configuration input))))

;; (defdatatest should-generate-k8s-domain [input expected]
;;   (is (= expected
;;          (sut/k8s-domain-configuration input))))
